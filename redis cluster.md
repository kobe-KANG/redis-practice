# Redis Cluster

- 데이터가 여러 Redis 노드에 자동으로 분산되어 저장되게 하고,
일부 노드에 장애가 발생하더라도 계속 작동할 수 있도록 고가용성(High Availability)을 제공하는 분산 시스템 구현체

- "용량을 확장(Sharding)"하고 "장애를 방지(Failover)"하기 위해 여러 대의 Redis 서버를 묶어서 사용하는 기술

### 1. 샤딩(Sharding)
- 데이터를 여러 노드에 나누어 저장하기 위해 **해시 슬롯(Hash Slot)**이라는 개념을 사용
- 모든 데이터(Key)는 특정 해시 슬롯에 매핑되며 클러스터 내의 마스터 노드들이 이 슬롯들을 나누어가짐
- 총 슬롯 개수 : 16,384개 ($2^{14}$)

### 2. Failover (마스터-슬레이브 구조)
- Master-Replica 구조: 각 마스터 노드는 하나 이상의 복제본(Replica/Slave) 노드를 가짐
- Failover: 마스터 노드 A가 죽으면, 자동으로 A의 복제본이 새로운 마스터로 승격되어 서비스를 지속


## Redis cluster 특징

### 자동 분할 (Automatic Sharding):
클라이언트는 데이터가 어느 노드에 저장될지 신경 쓸 필요 없이 키를 입력하면, 클러스터가 알아서 해당 슬롯이 있는 노드를 찾아 저장하거나 리다이렉트합니다.

### 무중단 운영 (Fault Tolerance):
일부 노드가 다운되어도 클러스터 전체는 멈추지 않고 작동합니다 (단, 과반수의 마스터가 생존해야 하는 등의 조건이 있습니다).

### 가십 프로토콜 (Gossip Protocol):
모든 노드는 서로(Mesh 구조) 주기적으로 상태를 주고받으며(Ping/Pong), 특정 노드가 살았는지 죽었는지 판단하고 슬롯 정보를 공유합니다.


